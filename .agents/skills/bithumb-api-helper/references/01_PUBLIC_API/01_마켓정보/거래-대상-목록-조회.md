# 거래 대상 목록 조회

빗썸에서 제공하는 거래 대상 페어(Trading Pairs) 목록과 관련 정보를 조회합니다.


---

## 거래 대상 목록 조회

빗썸에서 제공하는 거래 대상 페어(Trading Pairs) 목록과 관련 정보를 조회합니다.

| 항목 | 내용 |
|------|------|
| **Method** | `GET` |
| **URL** | `https://api.bithumb.com/v1/market/all` |
| **인증** | ❌ 인증 불필요 (Public) |
| **Content-Type** | `application/json` |

### 요청 파라미터

#### 쿼리 파라미터

| 파라미터 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| `isDetails` | boolean |  | `isDetails=true`로 요청하면 '유의 종목' 여부(`market_warning`)를 반환합니다.

  '주의 종목' 여부는 [경보제 조회 API](doc:경보제-조회)를 참고하시기 바랍니다. |

### 응답

#### `200` ✅ 성공

**응답 예시**

```json
"[\n    {\n        \"market\": \"KRW-BTC\",\n        \"korean_name\": \"비트코인\",\n        \"english_name\": \"Bitcoin\"\n    },\n    ...\n]"
```

**응답 필드** (배열 항목)

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `market` | string |  | 거래 대상 페어의 고유 심볼 (예시: KRW-BTC) |
| `korean_name` | string |  | 거래 대상 자산의 한글명 |
| `english_name` | string |  | 거래 대상 자산의 영문명 |
| `market_warning` | string |  | 유의 종목 여부   - `NONE`: 해당 사항 없음 - `CAUTION`: 거래 유의 (enum: `NONE`, `CAUTION`) |

#### `400` ❌ 오류

**응답 예시**

```json
"{\n    \"error\": {\n        \"name\": 400,\n        \"message\": \"Type mismatch error. Check the parameters type!\"\n    }\n}"
```

**응답 필드**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `error` | object | ✅ |  |

### 코드 샘플

#### Javascript

```javascript
const options = {method: 'GET', headers: {accept: 'application/json'}};

fetch('https://api.bithumb.com/v1/market/all?isDetails=false', options)
  .then(response => response.json())
  .then(response => console.log(response))
  .catch(err => console.error(err));
```

#### Python

```python
import requests

url = "https://api.bithumb.com/v1/market/all?isDetails=false"

headers = {"accept": "application/json"}

response = requests.get(url, headers=headers)

print(response.text)
```

#### Java

```java
OkHttpClient client = new OkHttpClient();

Request request = new Request.Builder()
  .url("https://api.bithumb.com/v1/market/all?isDetails=false")
  .get()
  .addHeader("accept", "application/json")
  .build();

Response response = client.newCall(request).execute();
```
